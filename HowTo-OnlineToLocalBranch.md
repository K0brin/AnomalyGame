1) open project in terminal
2) git checkout -b (name of new local branch) origin/(online branch name)
3) make sure local and online branch shares the same name
* if not rename branch with: git branch -m (old name) (new name)